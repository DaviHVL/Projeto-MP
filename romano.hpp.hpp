#include <iostream>
#include <string>

using namespace std;

//funcao para verificar se há apenas algarismos romanos
bool verificador_caracteres(const string& num_romano);

int romanos_para_decimal(const string& num_romano);

